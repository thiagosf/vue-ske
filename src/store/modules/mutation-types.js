const prefix = 'vuelab'

// Auth and User
export const LOGIN = `${prefix}/LOGIN`
export const SUCCESS_TOKEN = `${prefix}/SUCCESS_TOKEN`
export const INVALID_TOKEN = `${prefix}/INVALID_TOKEN`
export const LOGOUT = `${prefix}/LOGOUT`

// Notification
export const NOTIFY = `${prefix}/NOTIFY`
export const CLEAN_NOTIFICATION = `${prefix}/CLEAN_NOTIFICATION`
export const SET_NOTIFICATIONS = `${prefix}/SET_NOTIFICATIONS`

// i18n
export const SET_LOCALE = `${prefix}/SET_LOCALE`

// Transition
export const TRANSITION_NAME = `${prefix}/TRANSITION_NAME`
export const TRANSITION_MODE = `${prefix}/TRANSITION_MODE`
